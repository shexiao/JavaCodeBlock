package jdbc.rowset.filteredrowset;

/**
 * 
 * the created way is the same as JoinRowSet
 *
 * 0. extends WebRowSet
 * 1. apply filtering criteria so that only selected data is visible without connect to a data source
 */
public class Create {
	public static void main(String[] args) {
		
	}
}